{
  "name": "server",
  "version": "1.0.0",
  "description": "aplikasi untuk menyimpan data alamat teman-teman kelas smk asuuuuuu",
  "main": "src/server.js",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "cd client && npm run build && npm start",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-rate-limit": "^5.1.3",
    "helmet": "^4.1.1",
    "mongoose": "^5.10.6",
    "morgan": "^1.10.0",
    "rate-limit-mongo": "^2.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
